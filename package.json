{
  "name": "@hakari-ui-design-system/core",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=!@hakari-ui-design-system/docs && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.9",
    "@storybook/storybook-deployer": "^2.8.16",
    "turbo": "^2.1.1"
  },
  "packageManager": "npm@10.0.0",
  "dependencies": {
    "@radix-ui/react-tooltip": "^1.1.3"
  }
}
